server:
  port: 8080
spring:
  application:
    name: guest-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:guestdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
