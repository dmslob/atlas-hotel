server:
  port: 8082
spring:
  application:
    name: reservation-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:reservationdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
