server:
  port: 8081
spring:
  application:
    name: room-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:roomdb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
